<Page x:Class="Files.Views.Pages.FirstPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Files.Views.Pages"
      mc:Ignorable="d" 
      Title="{DynamicResource _app_name}" HorizontalAlignment="Center" VerticalAlignment="Center">

    <StackPanel Orientation="Vertical">

        <StackPanel Orientation="Horizontal">
            <Image Source="/Resources/Images/Application.png" Height="26" Width="26"/>
            <TextBlock Text="{DynamicResource _fp_name}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource SecundaryTextColor}" Margin="10"/>
        </StackPanel>

        <TextBlock Text="{DynamicResource _fp_title}" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0 15" Foreground="{DynamicResource PrimaryTextColor}"/>

        <Grid Margin="0 25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="15 0">
                <Image Source="/Resources/Images/PNG/login.png" Stretch="Uniform" Height="100" Width="100"/>
                <TextBlock Text="{DynamicResource _fp_label_1}" TextAlignment="Center" FontSize="11" Margin="0 10" FontWeight="SemiBold" Foreground="{DynamicResource SecundaryTextColor}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="15 0">
                <Image Source="/Resources/Images/PNG/analysis.png" Stretch="Uniform" Height="100" Width="100"/>
                <TextBlock Text="{DynamicResource _fp_label_2}" TextAlignment="Center" FontSize="11" Margin="0 10" FontWeight="SemiBold" Foreground="{DynamicResource SecundaryTextColor}"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Vertical" Margin="15 0">
                <Image Source="/Resources/Images/PNG/cyber-security.png" Stretch="Uniform" Height="100" Width="100"/>
                <TextBlock Text="{DynamicResource _fp_label_3}" TextAlignment="Center" FontSize="11" Margin="0 10" FontWeight="SemiBold" Foreground="{DynamicResource SecundaryTextColor}"/>
            </StackPanel>

        </Grid>

        <TextBlock Text="{DynamicResource _fp_label_4}" HorizontalAlignment="Center" Margin="0 15" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextColor}"/>

        <Grid Margin="0 15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="BtnLogin" Content="{DynamicResource _fp_btn_text_1}" Style="{DynamicResource BtnPrimaryStyle}" Click="BtnLogin_Click"/>
            <Button Grid.Column="1" x:Name="BtnRegister" Content="{DynamicResource _fp_btn_text_2}" Style="{DynamicResource BtnSecundaryStyle}" Click="BtnRegister_Click"/>
        </Grid>

        <TextBlock Text="{DynamicResource _copy_right}" Margin="0 10" HorizontalAlignment="Center" FontSize="11" Foreground="{DynamicResource SecundaryTextColor}"/>

    </StackPanel>
    
</Page>
